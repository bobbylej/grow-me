import { makeStyles } from '@material-ui/styles';

export const useResponseContainerStyles = makeStyles(() => ({
  responseContainer: {
    display: 'flex',
    justifyContent: 'space-evenly',
  },
  responseWrapper: {
    width: '30%',
  },
}));
