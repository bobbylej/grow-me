import { makeStyles } from '@material-ui/styles';

export const useLabelAnswerStyles = makeStyles(() => ({
  labelContainer: {
    display: 'flex',
    justifyContent: 'space-evenly',
  },
  labelWrapper: {
    width: '30%',
  },
}));
